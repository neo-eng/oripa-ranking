document.addEventListener('click',e=>{const a=e.target.closest('a[data-service-id]');if(!a)return;const root=a.closest('[data-lp-id]');const d={event:'affiliate_click',lp_id:root?.dataset.lpId||'',service_id:a.dataset.serviceId,service_name:a.closest('article')?.querySelector('h3')?.textContent||a.textContent.trim(),rank:Number(a.dataset.rank||0),cta_position:a.dataset.ctaPosition||'',traffic_source:new URLSearchParams(location.search).get('traffic')||'seo',click_datetime:new Date().toISOString()};window.dataLayer=window.dataLayer||[];window.dataLayer.push(d);});
document.addEventListener("DOMContentLoaded",()=>{document.querySelectorAll(".oripa-lp[data-sections]").forEach(root=>{const raw=(root.dataset.sections||"all").split(",").map(x=>x.trim());if(raw.includes("all"))return;const map={ranking:".oripa-ranking",comparison:".oripa-table",method:".oripa-info",summary:".oripa-info",sticky:".oripa-sticky"};Object.entries(map).forEach(([key,sel])=>{if(!raw.includes(key))root.querySelectorAll(sel).forEach(el=>el.hidden=true);});});});
document.addEventListener("DOMContentLoaded",()=>{document.querySelectorAll(".oripa-sticky").forEach(sticky=>{sticky.hidden=true;let shown=false;window.addEventListener("scroll",()=>{const y=window.scrollY;const nearBottom=window.innerHeight+window.scrollY>=document.documentElement.scrollHeight-180;const next=y>450&&!nearBottom;if(next!==shown){sticky.hidden=!next;shown=next;}},{passive:true});});});
